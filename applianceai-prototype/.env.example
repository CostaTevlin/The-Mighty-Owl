PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# GitHub Pages: add the same two names as repository secrets (Settings → Secrets and variables → Actions)
# so the "Deploy to GitHub Pages" workflow can run `npm run build` with a real Supabase URL.
